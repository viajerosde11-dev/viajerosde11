---
import { hero } from '../data/site';
---

<section id="inicio" class="relative">
  <div class="relative">
    <div class="relative mx-auto max-w-5xl px-6">
      <div class="relative overflow-hidden rounded-2xl shadow-xl">
        <video
          class="video-clean aspect-[16/9] w-full object-cover"
          autoplay
          muted
          loop
          playsinline
          preload="metadata"
          controlslist="nodownload nofullscreen"
          disablepictureinpicture
        >
          <source src="/videos/Beach.mp4" type="video/mp4" />
          Tu navegador no soporta el video.
        </video>
        <div class="absolute inset-x-0 bottom-0 bg-slate-900/55 px-6 py-3 text-white">
          <h3 class="text-lg font-semibold leading-snug md:text-xl">
            Hacemos realidad tu excursión de grado 11
          </h3>
        </div>
      </div>
    </div>
    <div class="mx-auto max-w-6xl px-6 py-10 text-center">
      <p class="text-base font-semibold uppercase tracking-[0.3em] text-ocean-dark md:text-lg">
        Agenda tu reunión o ve los planes
      </p>
      <div class="mt-4 flex flex-wrap justify-center gap-4">
        <a
          href={hero.ctas.primary.href}
          target="_blank"
          rel="noreferrer"
          class="inline-flex items-center justify-center rounded-full bg-coral px-6 py-3 text-sm font-semibold text-white shadow-lg transition hover:-translate-y-0.5 hover:bg-[#e85c3c]"
        >
          {hero.ctas.primary.label}
        </a>
        <a
          href={hero.ctas.secondary.href}
          class="inline-flex items-center justify-center rounded-full border border-ocean-dark px-6 py-3 text-sm font-semibold text-ocean-dark transition hover:-translate-y-0.5 hover:bg-white/70"
        >
          {hero.ctas.secondary.label}
        </a>
      </div>
    </div>
  </div>
</section>

